# GAS Mock mode for dev environment
# rest   — all calls are proxied to the Express REST API (default)
# static — all read calls return static data from gasMockData.ts (no REST requests)
VITE_MOCK_MODE=rest
